# AGENTS — example-app

Example Cursor project generated by AIDE.

## Workflow

Idea → Spec → Implement → Review → QA → Done

## Rules

See `.cursor/rules/` — start with `always.mdc`.

## Sample Feature

Spec: `docs/specs/sample-feature.md`
