# CLAUDE.md — example-app

You are a senior engineer on **example-app**. Follow spec-driven development.

## Rules

- Do not invent requirements
- Ask when spec is missing
- Follow `docs/architecture.md` for structure

## Workflow

Idea → Spec → Plan → Implement → Review → QA → Done

## Commands

See `commands/` — start with `/spec`.

## Sample Feature

Spec: `docs/specs/sample-feature.md`
